# web-console-starter-full · Claude & Codex 上架操作手册

> 让**别人能在 Claude Code / Codex 里搜索到并一键安装**你的插件。核心路径：**公开 Git 仓库（含 marketplace 声明）→ 把 `add owner/repo` 命令发给用户**。
> 官方目录（Anthropic 社区市场 / OpenAI Plugin Directory）是进阶路径，目前第三方提交有门槛，见文末"进阶：官方目录"。

## ⭐ 当前状态（2026-09-04 已上架）

| 平台 | GitHub 仓库 | 状态 |
|---|---|---|
| Claude | `https://github.com/peihongjun688/wcs-plugins` | ✅ 已公开 + manifest 已验证 |
| Codex | `https://github.com/peihongjun688/wcs-codex-plugins` | ✅ 已公开 + manifest 已验证 |

- 两个仓库均 Public、含 marketplace 声明、可被他人直接 `add`。
- "搜索到/评分"渠道：GitHub star + 第三方聚合站（claude-plugins.dev / claudeskills.info 等按 stars 排序自动抓取公开仓库）。
- 官方目录（claude-community / OpenAI Directory）未代提交，见第五节操作。

---

## 一、先把哪个仓库推上去

本目录已备好**两个现成的 marketplace 目录**，各自独立、结构合规：

| 平台 | 目录 | 内容 |
|---|---|---|
| **Claude** | `claude-marketplace/` | `.claude-plugin/marketplace.json` + `plugins/web-console-starter-full/`（含 `.claude-plugin/plugin.json` + 根级 SKILL.md + 11 张截图） |
| **Codex** | `codex-marketplace/` | `.agents/plugins/marketplace.json` + `plugins/web-console-starter-full/`（含 `.codex-plugin/plugin.json` + `skills/` + 11 张截图） |

> 两个目录已经过真机验证：Claude 版 `claude plugin validate` 通过；Codex 版 `codex plugin marketplace add` → `install` → `installed, enabled 1.2.1` 闭环。

**建议**：分别推两个仓库（干净、互不干扰），或推一个仓库放两个子目录（各自 add 子目录路径）。

---

## 二、Claude 上架步骤（让别人能搜到并安装）

### 2.1 推到 GitHub（2 分钟）

1. 打开 https://github.com/new
2. 仓库名建议：**`wcs-plugins`**（或 `claude-wcs-marketplace`），Public，README 勾上
3. 本机操作（以 `wcs-plugins` 为例）：
   ```bash
   cd "E:\Obsidian\nmphj-2026\00-共享\项目模板\web-console-starter-full-上架包\claude-marketplace"
   git init
   git add .
   git commit -m "web-console-starter-full v1.2.1 Claude plugin marketplace"
   git branch -M main
   git remote add origin https://github.com/<你的用户名>/wcs-plugins.git
   git push -u origin main
   ```

### 2.2 别人怎么加（粘贴给用户）

```text
在 Claude Code 里执行：

/plugin marketplace add <你的用户名>/wcs-plugins
/plugin install web-console-starter-full@wcs-plugins
/plugin reload-plugins
```

（`owner/repo` 支持 GitHub；也支持 `https://github.com/用户名/wcs-plugins.git` 或本地路径 `./claude-marketplace`）

- **装完后怎么触发**：直接描述任务即可（描述里写了触发词），Claude 会自动匹配；或显式 `@web-console-starter-full`。
- **Claude 里搜**：`/plugin` → 切换到对应 marketplace（或 Discover 页）就能看到插件条目（名称/描述/作者/组件）。

### 2.3 直达链接（给人看）

| 用途 | 链接/命令 |
|---|---|
| GitHub 仓库（人看介绍/star） | `https://github.com/peihongjun688/wcs-plugins` |
| 一键添加+安装 | `/plugin marketplace add peihongjun688/wcs-plugins` 然后 `/plugin install web-console-starter-full@wcs-plugins` |
| 官方社区市场提交（进阶） | 见文末 |

---

## 三、Codex 上架步骤

### 3.1 仓库与安装命令（已上架）

仓库：`https://github.com/peihongjun688/wcs-codex-plugins`

### 3.2 别人怎么装（粘贴给用户）

```
codex plugin marketplace add peihongjun688/wcs-codex-plugins
codex plugin list                      # 应看到 web-console-starter-full@wcs-codex-plugins
codex plugin add web-console-starter-full
```

- **装完触发**：Codex 里描述任务即可自动匹配；或 `/plugins` 目录里查看/启停。
- **Codex 里搜**：`/plugins` 打开插件目录 → 选择 `wcs-plugins` marketplace → 看插件(名称/描述/分类/截图)。

### 3.3 直达链接

| 用途 | 链接/方式 |
|---|---|
| GitHub 仓库 | `https://github.com/peihongjun688/wcs-codex-plugins` |
| 一键安装 | `codex plugin marketplace add peihongjun688/wcs-codex-plugins` 然后 `codex plugin add web-console-starter-full` |

---

## 四、别人"搜索到"的三种渠道

1. **直接把 add 命令贴给他们**（最快，官方推荐）：`/plugin marketplace add owner/repo` / `codex plugin marketplace add owner/repo` ——对方一条命令就装上。
2. **GitHub 搜索**：仓库名带 `wcs` / `web-console-starter` / `claude plugin` / `codex plugin` 关键词，别人在 GitHub 搜就能找到你仓库。
3. **官方目录（进阶）**：加入 Anthropic 社区市场 / OpenAI Plugin Directory 后，用户在官方目录内搜索。

---

## 五、官方目录（被"公开目录"收录）进阶路径

### Claude（Anthropic 官方两个市场）

| 市场 | 说明 | 怎么进 |
|---|---|---|
| **claude-plugins-official** | Anthropic 官方精选，无申请通道，官方自选 | 不能提交。被选中的用户会在 CLI 收到推荐 |
| **claude-community** | 公开社区市场，第三方提交**审核后**收录 | 提交表单（需先跑 `claude plugin validate .`） |

**提交表单（Claude 社区市场）**：
- Claude.ai：`https://claude.ai/settings/plugins/submit`（需 Team/Enterprise 组织+目录管理权限）
- Console（无组织也用这个）：`https://platform.claude.com/plugins/submit`

流程：本地 `claude plugin validate .` 通过 → 填表单（附仓库地址）→ 平台自动跑同样的校验+安全扫描 → 审核通过后 pin 到 `anthropics/claude-plugins-community` 目录 → 用户每晚同步后即可搜到/安装。**不是每位作者都能进官方精选**，社区市场是第三方主路。

### Codex（OpenAI Plugin Directory）

- 目前 **ChatGPT/Codex 通用插件目录第三方提交还是 "coming soon"**（官方 curated plugin directory 尚未完全开放第三方自助上架，官方文档明确"目录提交和自助发布即将推出"）。
- **现阶段第三方分发 = 自有 Git 市场**（上文 3.2），这是官方支持的公开渠道；等目录开放后再上官方目录。

---

## 六、维护与更新（版本迭代）

| 事情 | 操作 |
|---|---|
| 更新插件内容 | 改好 `plugins/web-console-starter-full/` 后，**`plugin.json` / `marketplace.json` 的 version 递增**（用户端只有 version 变才会收到更新） |
| Claude 发布 | `claude plugin validate .` 过审 → commit → push |
| Codex 发布 | `claude plugin` 那套不适用，直接 commit → push（用户 `codex plugin marketplace upgrade` 拉新） |
| 让老用户升级 | 发一条消息：`/plugin marketplace update`（Claude） / `codex plugin marketplace upgrade`（Codex） |

---

## 七、给别人看的介绍文案（复制即用）

### 一句话版
> **web-console-starter-full**：一套「Vue3 + ElementPlus + ECharts + Express + SQLite」的多皮肤中后台脚手架，桌面 4 皮肤 + 手机 H5 独立 6 皮肤、5 类双形态页面模板、一键打包，做软件著作权演示系统/批量业务系统的利器。

### 详细版（放 GitHub README / 帖子里）
> **web-console-starter-full v1.2.1**
> 一个能直接跑的中后台脚手架：**4 套桌面皮肤 + 6 套 H5 移动端皮肤（玻璃视觉 + 底部 TabBar + 设置页即时切换）**、5 类示范页面模板全部「桌面/H5 双形态」（≤768px 自动切 H5）、Express + SQLite 后端底座、工程脚本四件套（脚手架/加肤/命名校验/脱敏/打包）。适合做软件著作权演示系统、批量产出多套业务系统、需要手机访问的运维控制台。
> - 安装（Claude）：`/plugin marketplace add peihongjun688/wcs-plugins` → `/plugin install web-console-starter-full@wcs-plugins`
> - 安装（Codex）：`codex plugin marketplace add peihongjun688/wcs-codex-plugins` → `codex plugin add web-console-starter-full`
> - 插件内自带 11 张 H5 真机截图。

---

## 八、注意事项（踩过的坑）

1. **SkillHub 跟这两个完全无关**——SkillHub 是 WorkBuddy 的技能市场，格式、目录、命令都不同；本手册只管 Claude/Codex。
2. **先本地验证再分发**：Claude `claude plugin validate .`、Codex 临时 HOME 里 `add + list + install`，全过再发（见第 1 节两个目录已验过）。
3. **GitHub 仓库必须 public**（私有仓库别人 add 不了）。
4. **版本必须递增**：不改 version，对方永远收不到更新。
5. **几端不要混**：`.claude-plugin/`（Claude）≠ `.codex-plugin/`（Codex）≠ `.agents/plugins/`（Codex 市场）——别放错目录。